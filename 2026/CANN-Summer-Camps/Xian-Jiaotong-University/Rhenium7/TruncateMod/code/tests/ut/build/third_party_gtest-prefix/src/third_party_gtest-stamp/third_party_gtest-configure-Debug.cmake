
cmake_minimum_required(VERSION 3.15)

set(command "/opt/buildtools/cmake-3.20.5-linux-aarch64/bin/cmake;-DCMAKE_CXX_COMPILER=/usr/bin/c++;-DCMAKE_C_COMPILER=/usr/bin/cc;-DCMAKE_CXX_FLAGS=-D_GLIBCXX_USE_CXX11_ABI=0 -O2 -D_FORTIFY_SOURCE=2 -fPIC -fstack-protector-all -w;-DCMAKE_C_FLAGS=-D_GLIBCXX_USE_CXX11_ABI=0 -O2 -D_FORTIFY_SOURCE=2 -fPIC -fstack-protector-all -w;-DCMAKE_INSTALL_PREFIX=/mnt/workspace/code/tests/ut/build/3rd_party/gtest;-DCMAKE_INSTALL_LIBDIR=lib;-DBUILD_SHARED_LIBS=OFF;-Dgtest_build_tests=OFF;-Dgtest_build_samples=OFF;-Dgmock_build_tests=OFF;-GUnix Makefiles;/mnt/workspace/code/tests/ut/build/3rd_party/gtest-src")
set(log_merged "")
set(log_output_on_failure "")
set(stdout_log "/mnt/workspace/code/tests/ut/build/third_party_gtest-prefix/src/third_party_gtest-stamp/third_party_gtest-configure-out.log")
set(stderr_log "/mnt/workspace/code/tests/ut/build/third_party_gtest-prefix/src/third_party_gtest-stamp/third_party_gtest-configure-err.log")
execute_process(
  COMMAND ${command}
  RESULT_VARIABLE result
  OUTPUT_FILE "${stdout_log}"
  ERROR_FILE "${stderr_log}"
  )
macro(read_up_to_max_size log_file output_var)
  file(SIZE ${log_file} determined_size)
  set(max_size 10240)
  if (determined_size GREATER max_size)
    math(EXPR seek_position "${determined_size} - ${max_size}")
    file(READ ${log_file} ${output_var} OFFSET ${seek_position})
    set(${output_var} "...skipping to end...\n${${output_var}}")
  else()
    file(READ ${log_file} ${output_var})
  endif()
endmacro()
if(result)
  set(msg "Command failed: ${result}\n")
  foreach(arg IN LISTS command)
    set(msg "${msg} '${arg}'")
  endforeach()
  if (${log_merged})
    set(msg "${msg}\nSee also\n  ${stderr_log}")
  else()
    set(msg "${msg}\nSee also\n  /mnt/workspace/code/tests/ut/build/third_party_gtest-prefix/src/third_party_gtest-stamp/third_party_gtest-configure-*.log")
  endif()
  if (${log_output_on_failure})
    message(SEND_ERROR "${msg}")
    if (${log_merged})
      read_up_to_max_size("${stderr_log}" error_log_contents)
      message(STATUS "Log output is:\n${error_log_contents}")
    else()
      read_up_to_max_size("${stdout_log}" out_log_contents)
      read_up_to_max_size("${stderr_log}" err_log_contents)
      message(STATUS "stdout output is:\n${out_log_contents}")
      message(STATUS "stderr output is:\n${err_log_contents}")
    endif()
    message(FATAL_ERROR "Stopping after outputting logs.")
  else()
    message(FATAL_ERROR "${msg}")
  endif()
else()
  if(NOT "Unix Makefiles" MATCHES "Ninja")
    set(msg "third_party_gtest configure command succeeded.  See also /mnt/workspace/code/tests/ut/build/third_party_gtest-prefix/src/third_party_gtest-stamp/third_party_gtest-configure-*.log")
    message(STATUS "${msg}")
  endif()
endif()
